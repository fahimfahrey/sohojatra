#!/usr/bin/env sh

echo "→ lint-staged (eslint + secretlint on staged)"
npx lint-staged || exit 1

echo "→ typecheck"
npm run typecheck || exit 1

echo "→ npm audit (high+)"
npm run audit || { echo "✗ vulnerable deps. Run: npm run audit:fix"; exit 1; }

echo "✓ pre-commit ok"
